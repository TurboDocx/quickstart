{
  "eval_id": 9,
  "eval_name": "gin-both",
  "prompt": "I need both TurboSign and TurboPartner in my Gin project. Set up the TurboDocx SDK with both products.",
  "assertions": [
    {"name": "config-file-created", "type": "file_exists", "description": "A Go config/client file was created for TurboDocx"},
    {"name": "config-has-both-clients", "type": "file_contains", "description": "Config file creates both NewClientWithConfig (TurboSign) and NewPartnerClient (TurboPartner)"},
    {"name": "handler-files-created", "type": "file_exists", "description": "Handler files were created for signature and partner operations"},
    {"name": "uses-gin-context", "type": "file_contains", "description": "Handler functions use *gin.Context parameter (not http.ResponseWriter/http.Request)"},
    {"name": "routes-use-gin", "type": "file_contains", "description": "Routes registered using gin methods (r.POST, r.GET, router.Group) not http.HandleFunc"},
    {"name": "env-has-all-vars", "type": "file_contains", "description": ".env contains all 6 vars: TURBODOCX_API_KEY, ORG_ID, SENDER_EMAIL, SENDER_NAME, PARTNER_API_KEY, PARTNER_ID"},
    {"name": "uses-context", "type": "file_contains", "description": "Handler code uses context.Context or c.Request.Context() for SDK calls"},
    {"name": "follows-project-structure", "type": "file_exists", "description": "New files placed in internal/ directory (matching existing project structure)"}
  ]
}
